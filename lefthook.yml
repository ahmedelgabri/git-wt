pre-commit:
  parallel: true
  commands:
    govet:
      glob: "**/*.go"
      run: just vet
    treefmt:
      env:
        TERM: dumb
      run: just fmt-check

pre-push:
  commands:
    nix-build:
      run: just nix-build
