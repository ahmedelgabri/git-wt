pre-commit:
  parallel: true
  commands:
    govet:
      glob: "**/*.go"
      run: go vet ./...
    treefmt:
      env:
        TERM: dumb
      run: nix fmt -- --fail-on-change

pre-push:
  commands:
    nix-build:
      run: nix build --no-link
